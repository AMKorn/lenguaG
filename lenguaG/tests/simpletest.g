int a;
int b;
int c;
int[] d = [1, 2];
char[] codeAsk = "Introduce un número:";

main {
    out(codeAsk);
    in(a);
    out(codeAsk);
    in(b);
    out("El resultado es:");
    c = a + b;
    out(c);
}